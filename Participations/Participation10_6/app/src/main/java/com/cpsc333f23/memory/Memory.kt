package com.cpsc333f23.memory

import androidx.appcompat.app.AppCompatActivity
import com.cpsc333f23.gc3.R

class Memory (private var tiles: Int = 6, context: AppCompatActivity) {
        private val defaultBoardValue = context.getString(R.string.default_card_icon)
        /*println("Board initialized for ${tiles/2} matches!")*/
}